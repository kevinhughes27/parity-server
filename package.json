{
  "name": "parity-server",
  "private": true,
  "engines": {
    "node": "14.15.0",
    "yarn": "1.22.5"
  },
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "cd web && yarn build",
    "start": "cd web && yarn start"
  }
}
