{
  "name": "parity-server",
  "private": true,
  "engines": {
    "node": ">= 6.11.1",
    "yarn": ">= 0.24.5"
  },
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "cd web && yarn build",
    "start": "cd web && yarn start"
  }
}
